version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: fictional-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fictionalbank
    ports:
      - "5432:5432"
